<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Alpha Blog</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600&display=swap" rel="stylesheet">
  </head>

  <body style="min-height:100vh; margin:0; display:flex; flex-direction:column;
               font-family:'Poppins',sans-serif; background:linear-gradient(135deg,#f8f9fa 0%,#e9ecef 100%); color:#212529;">

    <!-- Hero Section -->
    <main style="flex:1; display:flex; align-items:center; justify-content:center; text-align:center; padding:60px 20px;">
      <div class="container" style="max-width:700px;">
        <h1 style="font-weight:600; font-size:3rem; color:#212529; margin-bottom:20px;">Alpha Blog</h1>
        <p style="font-size:1.1rem; color:#6c757d; margin-bottom:20px;">
          A clean and simple blogging platform built with Ruby on Rails.
        </p>
        <p style="font-size:0.95rem; color:#6c757d; margin-bottom:35px;">
          Share your thoughts, ideas, and projects with the world in a beautiful, minimal layout.
        </p>

        <div style="margin-bottom:30px;">
          <%= link_to 'Sign Up', '#', class: 'btn btn-success btn-lg', 
                style: 'border-radius:50px; padding:10px 30px; font-weight:500; transition:all 0.3s ease;' %>
        </div>

        <div style="display:flex; justify-content:center; gap:10px;">
          <%= button_to 'Articles Listing', articles_path, method: :get,
                class: 'btn btn-outline-primary btn-sm',
                style: 'border-radius:50px; padding:8px 18px; font-weight:500; transition:all 0.2s ease; margin-right:8px;' %>

          <%= button_to 'About Page', about_path, method: :get,
                class: 'btn btn-outline-secondary btn-sm',
                style: 'border-radius:50px; padding:8px 18px; font-weight:500; transition:all 0.2s ease;' %>
        </div>
      </div>
    </main>
  </body>
</html>
